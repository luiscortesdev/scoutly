[project]
name = "backend"
version = "0.1.0"
description = "Backend API and Algorithms"
requires-python = ">=3.14"
dependencies = []

# This registers terminal commands inside the uv environment
[project.scripts]


[tool.uv]
package = true